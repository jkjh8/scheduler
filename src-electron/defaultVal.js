const defaultValue = {
  mainServer: 'http://localhost:3000',
  mainStatus: false,
  backupServer: '',
  backupStatus: false,
  active: false,
  mode: 'main',
  auto: false,
  update: false,
  relayOnTime: 5,
  activeMode: 'main'
}

export default defaultValue
